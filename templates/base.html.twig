<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}Sunnah X Hall{% endblock %}</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    {% block stylesheets %}{% endblock %}

    <style>
      /* ===== NAVBAR VISIBLE SUR PAGES CLAIRES ===== */
.page-light .navbar-hero{
  position: sticky;         /* au lieu de absolute */
  top: 0;
  background: #111 !important;
  border-bottom: 1px solid rgba(255,255,255,.12);
}

.page-light .navbar-hero .nav-link,
.page-light .navbar-hero .navbar-brand{
  color: #fff !important;
}

.page-light .navbar-hero .navbar-toggler{
  filter: invert(1);
}

/* évite que le contenu passe sous la navbar */
.page-light main{
  padding-top: 24px;
}

    </style>
  </head>

  <body>
    {% block navbar %}
    <nav class="navbar navbar-expand-lg navbar-hero">
      <div class="container">
        <a class="navbar-brand fw-bold" href="#">sunnah_X_hall</a>

        <button class="navbar-toggler" type="button"
                data-bs-toggle="collapse" data-bs-target="#mainNav"
                aria-controls="mainNav" aria-expanded="false"
                aria-label="Menu">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mainNav">
          <ul class="navbar-nav ms-auto align-items-lg-center gap-lg-2">
            <li class="nav-item"><a class="nav-link" href="{{ path('app_home') }}">Accueil</a></li>
            <li class="nav-item"><a class="nav-link" href="#">Homme</a></li>
            <li class="nav-item"><a class="nav-link" href="#">Nouveautés</a></li>
            <li class="nav-item"><a class="nav-link" href="#">Contact</a></li>

            {# ✅ Partie AUTH #}
            {% if app.user %}
              <li class="nav-item ms-lg-3">
                <a class="nav-link" href="{{ path('app_panier_index') }}">Panier</a>
              </li>

              

              <li class="nav-item">
                <a class="btn btn-light" href="{{ path('app_logout') }}">Déconnexion</a>
              </li>
            {% else %}
              <li class="nav-item ms-lg-3">
                <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
              </li>

              <li class="nav-item">
                <a class="btn btn-light" href="{{ path('app_register') }}">Inscription</a>
              </li>
            {% endif %}
          </ul>
        </div>
      </div>
    </nav>
    {% endblock %}

    {% block body %}{% endblock %}

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    {% block javascripts %}{% endblock %}
  </body>
</html>
