{% extends 'basedeux.html.twig' %}

{% block title %}Edit Categorie{% endblock %}

{% block body %}
    <div class="text-center mb-5 mt-5 ">
        <h1 style="color: #0d1e3c;">Modifier Categorie</h1>
    </div>

    <div class="d-flex justify-content-center">
        <form class="p-4" style="
            width: 100%;
            max-width: 500px;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(13,30,60,0.2);
        ">
            {{ include('category/_form.html.twig', {'button_label': 'Update'}) }}
        </form>
    </div>

    <div class="d-flex justify-content-center mt-3">
        <a href="{{ path('app_category_index') }}" class="btn" style="
            background-color: #0d1e3c;
            color: #fff;
            border-radius: 5px;
            padding: 0.5rem 1.5rem;
        ">
            ⬅ Back to list
        </a>
    </div>

    <div class="d-flex justify-content-center mt-3">
        {{ include('category/_delete_form.html.twig') }}
    </div>
{% endblock %}
